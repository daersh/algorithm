#include <bits/stdc++.h>>

using namespace std;

int arr[100010] = {0};
int arr2[130000] = {0};
double MAX = -11111110;

int GO(int N);

int main() {
  ios_base ::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int n,m;
  cin >> n >> m;
int a;
  for (int i=0;i<n;i++){
    
    cin >> a;
    if(m>a) cout << a << ' ';
  }
}
