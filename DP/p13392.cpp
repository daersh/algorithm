#include <bits/stdc++.h>
using namespace std;




int main(){
   ios_base::sync_with_stdio(false);cin.tie(NULL); cout.tie(NULL);

    //나사 개수 
    int N;
    scanf("%d",N);
    int insert[N];
    int result[N];
    
    for(int i=0;i<N;i++){
        scanf("%1d",insert[i]);
    }
    for(int i=0;i<N;i++){
        scanf("%1d",result[i]);
    }

    // 왼쪽으로 갈지 오른쪽으로 갈지 ;
    
}